prompt_log_01
prompt:
/bva-domain-testing
/bva-domain-testing 
# FEATURE TO TEST: 1. Đăng ký tài khoản (FR01 - Account Registration)
## 1. Extracted Source Code Context
* **Endpoint:** `POST /api/register`
* **Database Constraints (database.js):**
  * `id` INTEGER PRIMARY KEY AUTOINCREMENT
  * `name` TEXT
  * `email` TEXT
  * `password` TEXT
  * `role` TEXT DEFAULT 'user'
  * `login_attempts` INTEGER DEFAULT 0
  * `locked_until` DATETIME
  * `reset_token` TEXT
  * `shipping_address` TEXT
  * `phone` TEXT
* **Seed Data:**
  * [admin@eshop.com](mailto:admin@eshop.com) (role = admin)
  * [test@eshop.com](mailto:test@eshop.com) (role = user)
* **Backend Validation Logic (server.js):**
  * Endpoint receives registration request through `POST /api/register`.
  * User information is stored in table `users`.
  * New users are assigned default role `'user'` if role is not explicitly provided.
  * Existing seeded emails indicate email uniqueness is expected.
  * No explicit validation for:
    * Email format
    * Password complexity
    * Name length
    * Phone format
    * Shipping address format
  * Validation rules not visible in provided source snippet should be marked as **Unknown**.
---
## 2. Input Data Mapping Specification (For AI Agent Skill)
### Numeric Fields
| Field          | Min | Max       | Notes                                                   |
| -------------- | --- | --------- | ------------------------------------------------------- |
| login_attempts | 0   | Undefined | Default value only. Not user input during registration. |
### String Fields & Limits
| Field            | Required         | Min Length | Max Length | Notes                                                                               |
| ---------------- | ---------------- | ---------- | ---------- | ----------------------------------------------------------------------------------- |
| name             | Unknown          | Undefined  | Undefined  | User registration field. No validation found.                                       |
| email            | Expected Yes     | Undefined  | Undefined  | Existing records suggest uniqueness requirement. Email format validation not found. |
| password         | Expected Yes     | Undefined  | Undefined  | No password length/complexity validation found in provided code.                    |
| role             | No               | N/A        | N/A        | Default = 'user'.                                                                   |
| shipping_address | No               | Undefined  | Undefined  | No validation found.                                                                |
| phone            | No               | Undefined  | Undefined  | No validation found.                                                                |
| reset_token      | System Generated | N/A        | N/A        | Not registration input.                                                             |
### Enum Values
| Field | Allowed Values                          |
| ----- | --------------------------------------- |
| role  | user, admin (identified from seed data) |
### Inter-field Dependencies
| Dependency | Rule                                                     |
| ---------- | -------------------------------------------------------- |
| role       | If omitted, system should assign default value `'user'`. |
| email      | Should not duplicate an existing user email.             |
| id         | Auto-generated by database.                              |
### Error Response Format
**Unknown from provided source code.**
Potential API error patterns to verify from implementation:
```json
{
  "error": "message"
}
```
Possible scenarios:
* Missing required field
* Duplicate email
* Invalid payload format
* Internal server error
Status codes and exact error messages cannot be confirmed from the provided source snippet.
AI-output:
## 1. Feature Variables & Domain Decomposition

**Endpoint:** `POST /api/register`
**Content-Type:** `application/json`

| Variable | Type | Format | Constraints | Dependencies |
|----------|------|--------|-------------|--------------|
| `name` | TEXT | Free string | Required: **Unknown**. Min/Max length: **Unknown** | None |
| `email` | TEXT | Email string | Required: Expected YES. Uniqueness enforced (seed data evidence). Format validation: **Unknown** | Must not duplicate existing record |
| `password` | TEXT | Free string | Required: Expected YES. Min/Max length: **Unknown**. Complexity: **Unknown** | None |
| `role` | TEXT | Enum | Optional. Default = `'user'`. Allowed: `user`, `admin` | If omitted → system sets `'user'` |
| `shipping_address` | TEXT | Free string | Optional. No validation | None |
| `phone` | TEXT | Free string | Optional. No validation | None |

**Fields NOT in scope (system-managed):** `id` (AUTOINCREMENT), `login_attempts` (DEFAULT 0), `locked_until`, `reset_token`

> **Assumption A1:** `email` and `password` are required fields — inferred from seed data patterns and standard registration logic. No explicit validation code is confirmed.

> **Assumption A2:** No max length constraints exist unless SQLite TEXT default limits apply (practically unlimited). All string length boundaries are derived from industry-standard conservative defaults.

> **Assumption A3:** The API returns JSON. Error format assumed to be `{"error": "<message>"}` based on spec hint. HTTP status codes are inferred from REST conventions since none are explicitly defined.

> **Assumption A4:** `name` is treated as required for registration UX — though not confirmed. Tests will cover both present and absent scenarios.

> **Assumption A5:** Sending `role: "admin"` in the request body is a mass-assignment risk. System should ignore or reject it.

---

## 2. BVA Derivation & Rationale (Step-by-Step)

### Variable: `email`
```
Type: string (email format)
Min length boundary (conservative min = 6 chars: a@b.co):
  Min-1 = ""           → INVALID (empty)
  Min   = "a@b.co"     → VALID (shortest plausible email)
  Min+1 = "ab@b.co"    → VALID

Nominal = "user@example.com"

Max length (RFC 5321 = 254 chars):
  Max-1 = 253-char valid email → VALID
  Max   = 254-char valid email → VALID
  Max+1 = 255-char email       → INVALID (exceeds RFC limit)

Special:
  null / missing         → INVALID (required)
  duplicate email        → INVALID (uniqueness violated)
  no @ symbol            → INVALID format
  no domain              → INVALID format ("user@")
  no local part          → INVALID format ("@example.com")
  seeded email           → INVALID (admin@eshop.com, test@eshop.com)
  SQL injection payload  → Should sanitize
  XSS payload            → Should sanitize
```

> **Assumption A6:** RFC 5321 max email length of 254 chars is used as the practical upper bound since no app-level constraint is defined.

### Variable: `password`
```
Type: string
No explicit min/max defined.

Conservative assumption: min = 1 char (non-empty required)
  Min-1 = ""        → INVALID (empty, if required)
  Min   = "a"       → VALID (1 char — edge, system-dependent)
  Min+1 = "ab"      → VALID (2 chars)

Nominal = "Password123!"

Max (SQLite TEXT, no app limit → use 1000 chars as stress boundary):
  Max-1 = 999-char string  → VALID
  Max   = 1000-char string → VALID (stress test)
  Max+1 = 10001-char string → VALID or system-dependent (no rejection expected without explicit limit)

Special:
  null / missing             → INVALID (required)
  spaces only "   "          → Edge — system-dependent
  unicode / emoji "密码🔑"   → Edge — system-dependent
  SQL injection payload      → Should sanitize
```

> **Assumption A7:** Password minimum length = 1 (non-empty). No complexity rules. Tests at 1 char will reveal if server enforces any hidden minimum.

### Variable: `name`
```
Type: string
No validation defined. Required status unknown.

Conservative assumption: min = 1 char if required.
  Min-1 = ""      → behavior unknown (may accept or reject)
  Min   = "A"     → VALID (1 char)
  Min+1 = "AB"    → VALID

Nominal = "John Doe"

Max: undefined → stress test at 500 chars
  Special:
    null / missing         → behavior unknown
    digits only "12345"    → likely valid (no format rule)
    special chars "O'Brien" → likely valid
    XSS payload            → Should sanitize
    emoji "😊"             → system-dependent
```

### Variable: `role`
```
Type: TEXT ENUM
Allowed: 'user', 'admin'
Default: 'user' when omitted

Valid:
  Omitted (not in payload)   → system assigns 'user'
  "user" explicitly           → VALID

Invalid / Attack vectors:
  "admin"                    → INVALID input (mass assignment — system must reject/ignore)
  "superuser"                → INVALID (not in enum)
  ""  (empty string)         → INVALID
  null                       → should default to 'user'
  123 (integer type)         → INVALID type
```

### Variable: `phone` (optional)
```
Type: string, optional, no format validation
  "" (empty string)           → Likely valid (optional)
  null / omitted              → Valid (optional)
  "0901234567"                → Valid nominal
  "+84901234567"              → Valid
  "not-a-phone-number"        → Valid (no format validation confirmed)
  SQL injection payload       → Should sanitize
```

### Variable: `shipping_address` (optional)
```
Type: string, optional, no validation
  "" (empty string)   → Likely valid
  null / omitted      → Valid
  Nominal = "123 Main St, HCMC"
  Long address (500 chars) → Stress test
  XSS payload         → Should sanitize
```

---

## 3. Comprehensive Test Case Matrix

| Test ID | Title | Preconditions | Inputs (exact values) | Steps | Expected Result | HTTP Status |
|---------|-------|---------------|----------------------|-------|-----------------|-------------|
| TC-REG-001 | Happy path — valid full registration | Email `newuser@test.com` not in DB | `name="John Doe"`, `email="newuser@test.com"`, `password="Password123!"`, `phone="0901234567"`, `shipping_address="123 Main St"` | POST /api/register with full valid body | User created, response with user info or success message | 201 |
| TC-REG-002 | Happy path — minimal required fields only | Email `minimal@test.com` not in DB | `name="Jane"`, `email="minimal@test.com"`, `password="pass"` | POST /api/register with only required fields | User created with `role='user'` default | 201 |
| TC-REG-003 | Role defaults to 'user' when omitted | Email `roletest@test.com` not in DB | `name="RoleTest"`, `email="roletest@test.com"`, `password="pass123"` (no `role` field) | POST → check DB or response | `role` value = `'user'` | 201 |
| TC-REG-004 | Duplicate email — seeded admin | DB has `admin@eshop.com` | `name="Test"`, `email="admin@eshop.com"`, `password="pass123"` | POST /api/register | Error: email already exists | 400 / 409 |
| TC-REG-005 | Duplicate email — seeded user | DB has `test@eshop.com` | `name="Test"`, `email="test@eshop.com"`, `password="pass123"` | POST /api/register | Error: email already exists | 400 / 409 |
| TC-REG-006 | Duplicate email — self-registered | Register `dup@test.com` first | `name="Dup"`, `email="dup@test.com"`, `password="pass123"` | POST /api/register (2nd time same email) | Error: email already exists | 400 / 409 |
| TC-REG-007 | Missing `email` field | Clean DB | `name="NoEmail"`, `password="pass123"` | POST without email field | Error: email required | 400 |
| TC-REG-008 | Missing `password` field | Clean DB | `name="NoPass"`, `email="nopass@test.com"` | POST without password field | Error: password required | 400 |
| TC-REG-009 | Empty string `email` | Clean DB | `name="A"`, `email=""`, `password="pass123"` | POST /api/register | Error: invalid email | 400 |
| TC-REG-010 | Empty string `password` | Clean DB | `name="A"`, `email="emptypass@test.com"`, `password=""` | POST /api/register | Error: password required/invalid | 400 |
| TC-REG-011 | `email` = null | Clean DB | `name="A"`, `email=null`, `password="pass123"` | POST /api/register | Error: email required | 400 |
| TC-REG-012 | `password` = null | Clean DB | `name="A"`, `email="nullpass@test.com"`, `password=null` | POST /api/register | Error: password required | 400 |
| TC-REG-013 | Email without `@` symbol | Clean DB | `name="A"`, `email="invalidemail.com"`, `password="pass123"` | POST /api/register | Error: invalid email format (if validation exists) | 400 |
| TC-REG-014 | Email with no domain | Clean DB | `name="A"`, `email="user@"`, `password="pass123"` | POST /api/register | Error: invalid email format (if validation exists) | 400 |
| TC-REG-015 | Email with no local part | Clean DB | `name="A"`, `email="@example.com"`, `password="pass123"` | POST /api/register | Error: invalid email format (if validation exists) | 400 |
| TC-REG-016 | Email at RFC min length (6 chars) | Clean DB | `name="A"`, `email="a@b.co"`, `password="pass123"` | POST /api/register | Success — user created | 201 |
| TC-REG-017 | Email at 254 chars (RFC max) | Clean DB | `name="A"`, `email="<aaa...253chars>@b.co"` (254 total), `password="pass123"` | POST /api/register | Success or length-limit error | 201 / 400 |
| TC-REG-018 | Email at 255 chars (RFC max+1) | Clean DB | `name="A"`, `email="<254 chars>x@b.co"` (255 total), `password="pass123"` | POST /api/register | Error: email too long | 400 |
| TC-REG-019 | Password = 1 character (Min) | Email `pw1@test.com` not in DB | `name="A"`, `email="pw1@test.com"`, `password="a"` | POST /api/register | Success or min-length error | 201 / 400 |
| TC-REG-020 | Password = 1000 chars (stress Max) | Email `pw1000@test.com` not in DB | `name="A"`, `email="pw1000@test.com"`, `password="<1000 x 'a'>"` | POST /api/register | Success (no length limit enforced) | 201 |
| TC-REG-021 | `name` = empty string | Email `emptyname@test.com` not in DB | `name=""`, `email="emptyname@test.com"`, `password="pass123"` | POST /api/register | Success (if optional) or error (if required) | 201 / 400 |
| TC-REG-022 | `name` = null | Email `nullname@test.com` not in DB | `name=null`, `email="nullname@test.com"`, `password="pass123"` | POST /api/register | Success (nullable) or error | 201 / 400 |
| TC-REG-023 | `name` = 1 char (Min) | Email `name1@test.com` not in DB | `name="A"`, `email="name1@test.com"`, `password="pass123"` | POST /api/register | Success | 201 |
| TC-REG-024 | `name` = 500 chars (stress) | Email `namelong@test.com` not in DB | `name="<500 x 'A'>"`, `email="namelong@test.com"`, `password="pass123"` | POST /api/register | Success (no length limit) | 201 |
| TC-REG-025 | `role` omitted → defaults to 'user' | Email `roledefault@test.com` not in DB | `name="A"`, `email="roledefault@test.com"`, `password="pass123"` (no role) | POST → verify role in DB | role = `'user'` in DB | 201 |
| TC-REG-026 | `role` = 'user' explicitly | Email `roleuser@test.com` not in DB | `name="A"`, `email="roleuser@test.com"`, `password="pass123"`, `role="user"` | POST /api/register | Success, role = `'user'` | 201 |
| TC-REG-027 | Mass assignment — `role` = 'admin' | Email `roleadmin@test.com` not in DB | `name="A"`, `email="roleadmin@test.com"`, `password="pass123"`, `role="admin"` | POST /api/register | role stored as `'user'` (ignored) OR 400 error | 201 (role='user') / 400 |
| TC-REG-028 | `role` = invalid enum value | Email `roleinvalid@test.com` not in DB | `name="A"`, `email="roleinvalid@test.com"`, `password="pass123"`, `role="superuser"` | POST /api/register | Error or stored as-is (gap — no enum validation confirmed) | 400 / 201 |
| TC-REG-029 | `phone` optional — omitted | Email `nophone@test.com` not in DB | `name="A"`, `email="nophone@test.com"`, `password="pass123"` (no phone) | POST /api/register | Success, phone = null in DB | 201 |
| TC-REG-030 | `phone` = valid nominal | Email `phone@test.com` not in DB | `name="A"`, `email="phone@test.com"`, `password="pass123"`, `phone="0901234567"` | POST /api/register | Success | 201 |
| TC-REG-031 | `phone` = non-numeric string | Email `phonestr@test.com` not in DB | `name="A"`, `email="phonestr@test.com"`, `password="pass123"`, `phone="not-a-phone"` | POST /api/register | Success (no format validation) | 201 |
| TC-REG-032 | `shipping_address` optional — omitted | Email `noaddr@test.com` not in DB | `name="A"`, `email="noaddr@test.com"`, `password="pass123"` | POST /api/register | Success, address = null in DB | 201 |
| TC-REG-033 | Empty request body `{}` | Clean DB | `{}` (empty JSON) | POST /api/register | Error: missing required fields | 400 |
| TC-REG-034 | Non-JSON content type | Clean DB | Raw text body: `name=A&email=a@b.co&password=pass` | POST with `Content-Type: text/plain` | Error: invalid content type | 400 / 415 |
| TC-REG-035 | Concurrent duplicate registration | Email `race@test.com` not in DB | Two simultaneous POST with `email="race@test.com"` | Fire 2 requests at same time | Only 1 user created; 2nd gets duplicate error | 201 + 400/409 |

---

## 4. AI Gap Analysis & Vulnerability Report

### 4.1 Specification Gaps

| Gap ID | Description | Affected Variables | Risk Level | Recommendation |
|--------|-------------|-------------------|------------|----------------|
| GAP-001 | No explicit `required` validation for `name` — behavior on null/missing is undefined | `name` | HIGH | Define whether `name` is required. Add server-side guard: `if (!name) return 400` |
| GAP-002 | No email format validation found in source — structurally invalid emails may be stored | `email` | HIGH | Add regex or library validation (e.g., `validator.isEmail()`) |
| GAP-003 | No password complexity or minimum length rules — 1-char passwords accepted | `password` | HIGH | Enforce minimum 8 chars and at minimum disallow empty string |
| GAP-004 | No error response format specified — status codes and body structure are unknown | All fields | HIGH | Document and standardize: `{"error": "message"}` + explicit HTTP codes |
| GAP-005 | `role` field accepts user-supplied values — mass assignment of `role="admin"` not explicitly blocked | `role` | CRITICAL | Whitelist: ignore `role` from request body entirely; always set `DEFAULT 'user'` |
| GAP-006 | No unique constraint enforcement confirmed at application layer — only implied by seed data | `email` | HIGH | Add `UNIQUE` constraint to `email` column in DB schema; handle `SQLITE_CONSTRAINT` error gracefully |
| GAP-007 | Race condition on concurrent registration with same email not handled | `email` | MEDIUM | Use DB-level `UNIQUE` + catch constraint error; add idempotency handling |
| GAP-008 | No max length constraint on any TEXT field — potential DoS via large payload | `name`, `password`, `phone`, `shipping_address` | MEDIUM | Add `maxLength` validation at middleware level (e.g., 255 for name, 72 for password bcrypt limit) |
| GAP-009 | `role` enum not validated — values like `"superuser"` or `"moderator"` may be stored | `role` | MEDIUM | Validate against allowlist `['user', 'admin']`; reject unknown values |
| GAP-010 | No `Content-Type: application/json` enforcement — non-JSON payloads may cause unhandled errors | All fields | LOW | Add `express.json()` middleware with strict mode or check `Content-Type` header |

---

### 4.2 Security Vulnerability Test Cases

| Vuln ID | Category | Test Input | Attack Vector | Expected Safe Response |
|---------|----------|-----------|---------------|----------------------|
| SEC-001 | SQL Injection | `email="' OR '1'='1' --"`, `password="x"` | `email` field in INSERT query | 400 Bad Request; no DB error exposed |
| SEC-002 | SQL Injection | `name="Robert'); DROP TABLE users;--"`, `email="drop@test.com"`, `password="x"` | `name` field | 400 or stored as literal string; table not dropped |
| SEC-003 | XSS — Stored | `name="<script>alert('xss')</script>"`, `email="xss@test.com"`, `password="x"` | `name` field (rendered in UI later) | Stored as escaped string; not executed on retrieval |
| SEC-004 | XSS — Stored | `shipping_address="<img src=x onerror=alert(1)>"`, `email="xss2@test.com"`, `password="x"` | `shipping_address` field | Stored escaped; not executed |
| SEC-005 | Mass Assignment — Privilege Escalation | `role="admin"`, `email="massassign@test.com"`, `password="x"` | `role` field in body | `role` stored as `'user'` (ignored) OR 400 |
| SEC-006 | Mass Assignment — System Fields | `id=9999`, `login_attempts=0`, `reset_token="abc"`, `email="massfield@test.com"`, `password="x"` | Injecting system-managed fields | Extra fields ignored; system fields not overwritten |
| SEC-007 | Oversized Payload / DoS | `password="<72001 chars>"` | `password` field | bcrypt silently truncates at 72 chars OR server rejects; no crash |
| SEC-008 | Null Byte Injection | `email="user%00@test.com"`, `password="x"` | `email` field | 400 Bad Request; null byte stripped or rejected |
| SEC-009 | Unicode / Homograph Attack | `email="аdmin@eshop.com"` (Cyrillic `а`) | `email` field (looks like admin) | Treated as new unique email; not confused with `admin@eshop.com` |
| SEC-010 | Auth Bypass — Extra Headers | `X-User-Role: admin` header added to request | HTTP header injection attempt | Header ignored; role always defaults to `'user'` |

---

### 4.3 Enhancement Test Cases (Gap Closure)

| TC ID | Gap Addressed | Title | Input | Expected Result |
|-------|--------------|-------|-------|-----------------|
| TC-REG-ENH-001 | GAP-002 | Email without TLD | `email="user@domain"` | 400 — invalid email format |
| TC-REG-ENH-002 | GAP-002 | Email with spaces | `email="user @example.com"` | 400 — invalid email format |
| TC-REG-ENH-003 | GAP-003 | Password = spaces only | `password="   "` (3 spaces) | 400 — password cannot be whitespace only |
| TC-REG-ENH-004 | GAP-008 | Name > 255 chars | `name="<256 x 'A'>"` | 400 — name too long (after fix) |
| TC-REG-ENH-005 | GAP-008 | Password > 72 chars (bcrypt risk) | `password="<73 x 'a'>"` | Accepted but warn: bcrypt silently truncates at 72; two different passwords may hash identically |
| TC-REG-ENH-006 | GAP-005 | `role=admin` must be ignored | `role="admin"` in body | role = `'user'` in DB regardless |
| TC-REG-ENH-007 | GAP-007 | Race condition — same email x2 | Two concurrent POSTs with `email="race@test.com"` | Exactly 1 row in DB; 2nd request gets 409 |
| TC-REG-ENH-008 | GAP-006 | DB uniqueness on email case-sensitivity | Register `User@Test.com` then `user@test.com` | System should treat as duplicate (case-insensitive check recommended) |

---

**Summary:** 35 functional test cases + 10 security test cases + 8 enhancement test cases = **53 total test cases** covering all BVA boundaries, equivalence partitions, cross-variable dependencies, and attack vectors for `POST /api/register`.